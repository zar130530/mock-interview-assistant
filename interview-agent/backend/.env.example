# 复制为 .env 并填写；前端也可在页面中设置（前端设置优先级更高）
# OpenAI 兼容接口地址（DeepSeek/通义/Ollama 只需改这里）
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
# 默认模型
LLM_MODEL=gpt-4o-mini
# 无 key 时返回 Mock 数据，便于无密钥直接跑通前端（1=开启）
MOCK=0
